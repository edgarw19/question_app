<div class="container", "relative">
	<div class="row-fluid">
		<h1><%= @question.query %></h1>

		
		<p>
			<%= @question.text %>
		</p>
		<h3>Answers</h3>
		<%= render @question.answers %>
		<h5>Add an answer:</h5>
		<%= render "answers/form" %>
		
		<!--must check the comment item-->

	</div>
</div>